# Usecase 2 - Ingest data from a Postgres database

In this lab we will use the Postgres Connector and Change Data Capture to build a workflow that ingests data from a Postgres database into Snowflake.
